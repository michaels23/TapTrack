<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.example.carappskeleton">

	<uses-sdk android:minSdkVersion="24" android:targetSdkVersion="33" />

	<application
		android:allowBackup="true"
		android:label="CarAppSkeleton"
		android:icon="@mipmap/ic_launcher"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:theme="@style/Theme.CarAppSkeleton">
		<activity android:name=".MainActivity" android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<!-- Do not declare the MAUI app's receiver here. The receiving BroadcastReceiver
			 (CarTapReceiver) is defined in the MAUI app package (com.companyname.taptrack).
			 Declaring it here causes Android Studio to look for a local class and will report
			 unresolved-class warnings. The sender should make the intent explicit instead.
		-->
	</application>

</manifest>